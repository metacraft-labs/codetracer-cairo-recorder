# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-cairo-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-cairo-recorder (CodeTracer recorder for Cairo and StarkNet smart contracts).

pkgname=codetracer-cairo-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for Cairo and StarkNet smart contracts"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-cairo-recorder"
license=('Apache-2.0')
depends=('glibc' 'zstd')
makedepends=('rust' 'cargo')
provides=("codetracer-cairo-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-cairo-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-cairo-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-cairo-recorder
}

check() {
  cd "$srcdir/codetracer-cairo-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-cairo-recorder || true
}

package() {
  cd "$srcdir/codetracer-cairo-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-cairo-recorder" \
    "$pkgdir/usr/bin/codetracer-cairo-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
